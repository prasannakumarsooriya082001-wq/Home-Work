class RangeCheck
{
	public static void main(String[] args)
	{
		int num = 20;
		
		
		String range = ((num >= 10) && (num <=50)) ? "Number is in Range" : "Number is Not in Range";
		
		System.out.println(range);
	}
}